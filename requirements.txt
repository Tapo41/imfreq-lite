# ImFREQ-Lite v3 — requirements.txt
# Install with: pip install -r requirements.txt
#
# v3 additions vs v1:
#   - torch      : required for TCN and Quantized Transformer baselines
#   - river      : ADWIN concept drift detection (optional; built-in fallback included)
#   - psutil     : RAM profiling for ARM deployment
#   - joblib     : model serialisation for ARM deployment (usually ships with sklearn)

# ── Core scientific stack ─────────────────────────────────────────────────────
numpy==1.24.4
scipy==1.11.4
pandas==2.1.4

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn==1.3.2
xgboost==2.0.3
lightgbm==4.1.0
imbalanced-learn==0.11.0

# ── Deep learning (baselines only) ───────────────────────────────────────────
# TensorFlow — LSTM Autoencoder baseline
tensorflow==2.13.0

# PyTorch — TCN and Quantized Transformer baselines (v3 new)
# CPU-only install recommended for Colab and Raspberry Pi:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ── Streaming & drift detection ───────────────────────────────────────────────
# river — full ADWIN implementation (Bifet & Gavalda, SDM 2007)
# A built-in KS-based ADWIN approximation is included if river is not installed.
river>=0.21.0

# ── ARM deployment profiling ──────────────────────────────────────────────────
psutil>=5.9.0        # RAM profiling via psutil.Process().memory_info().rss
joblib==1.3.2        # model serialisation (also ships with scikit-learn)

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.8.2
seaborn==0.13.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.66.1
